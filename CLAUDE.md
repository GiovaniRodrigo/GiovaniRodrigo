# CLAUDE.md

Guidance for AI agents working in the `GiovaniRodrigo/GiovaniRodrigo` GitHub **profile** repo
(its `README.md` renders on the GitHub profile page).

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues (`gh` CLI), repo `GiovaniRodrigo/GiovaniRodrigo`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`), label strings equal to their names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
